apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "resources.fullname" . }}
  labels:
    {{- include "resources.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "resources.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "resources.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "resources.fullname" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: DRYCC_CONTROLLER_URL
              value: {{ .Values.controller.url | quote }}
            - name: DRYCC_CONTROLLER_VERIFY_TLS
              value: {{ .Values.controller.verifyTLS | quote }}
            - name: DRYCC_CONTROLLER_AUTH_CACHE_TTL
              value: {{ .Values.controller.authCacheTTL | quote }}
            - name: DRYCC_DATABASE_URL
              value: {{ .Values.database.url | quote }}
            {{- with .Values.valkey.url }}
            - name: DRYCC_VALKEY_URL
              value: {{ . | quote }}
            {{- end }}
            - name: K8S_API_VERIFY_TLS
              value: {{ .Values.kubernetes.apiVerifyTLS | quote }}
            - name: WORKFLOW_NAMESPACE
              value: {{ .Values.kubernetes.namespace | default .Release.Namespace | quote }}
            {{- with .Values.workflowManager.url }}
            - name: WORKFLOW_MANAGER_URL
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.workflowManager.accessKey }}
            - name: WORKFLOW_MANAGER_ACCESS_KEY
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.quickwit.indexerUrl }}
            - name: QUICKWIT_INDEXER_URL
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.quickwit.logIndexPrefix }}
            - name: QUICKWIT_LOG_INDEX_PREFIX
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.secretKey }}
            - name: DRYCC_SECRET_KEY
              value: {{ . | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /resources/services
              port: http
          readinessProbe:
            httpGet:
              path: /resources/services
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
